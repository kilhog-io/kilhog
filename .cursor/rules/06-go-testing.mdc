---
description: Unit testing requirements for Go code
globs: "*.go"
alwaysApply: false
---

# AUTOMATED TESTS

- For each new handler or service created, generate the corresponding `_test.go` file.
- Use Go's idiomatic **Table-Driven Tests** approach.
- Use the standard `testing` library or `stretchr/testify` for assertions.
