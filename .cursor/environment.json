{
  "name": "kilhog",
  "user": "ubuntu",
  "install": "go mod download && make build-all",
  "terminals": [
    {
      "name": "kilhog-server",
      "command": "make run-dev",
      "description": "kilhog REST API server (SQLite, API key auth) on http://localhost:8080. Health: GET /healthz."
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8080
    }
  ]
}
